pub mod arch32;
pub mod arch64;

/* 
    pub enum Data64 {
        Addr = u64,
        Off = u64,
        Half = u16,
        Word = u32,
        SWord = i32,
        XWord = u64,
        SXWord = i64,
        UChar = u8,
    }

    struct Header64 {
        pub address : Data64::Addr,
        pub etype : Data64::Half,
    }
*/